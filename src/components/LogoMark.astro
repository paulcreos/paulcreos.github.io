---
import crestLogoUrl from '../images/CREOS-logo-warm.svg?url';

interface Props {
  class?: string;
  alt?: string;
}

const { class: className, alt = '' } = Astro.props;
---

<span class:list={['logo-mark', className]}>
  <img src={crestLogoUrl} alt={alt} />
</span>
